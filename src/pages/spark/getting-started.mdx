---
title: Getting Started with Delta Lake Spark
description: Learn how to start using Delta Lake Spark
---

Lorem ipsum dolor sit amet.

<CodeTabs>

```jsx
export const MyComponent = (props) => {
  const { title, children } = props;

  return (
    <div>
      <h1>{title}</h1>
      <p>{children}</p>
    </div>
  );
};
```

```tsx
import type { ReactNode } from "react";

interface MyComponentProps {
  title: string;
  children?: ReactNode;
}

export const MyComponent = (props): ReactElement => {
  const { title, children } = props;

  return (
    <div>
      <h1>{title}</h1>
      <p>{children}</p>
    </div>
  );
};
```

</CodeTabs>
